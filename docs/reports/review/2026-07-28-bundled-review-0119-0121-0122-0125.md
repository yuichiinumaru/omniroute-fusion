# Bundled Review Report: Tasks 0119, 0121, 0122, 0125 — 2026-07-28

> **Date**: 2026-07-28
> **Reviewer lane**: `reviewers`
> **Profile**: TypeScript Expert — Implacable Semantic Auditor (omniroute/claudao)
> **Review mode**: Re-review (0119, 0121) + Initial (0122, 0125) — ultra-rigorous per user request
> **Bundled**: yes — independent scores, shared blast-radius section

---

## Review Lineage

- **Previous reports read**:
  - `docs/reports/review/2026-07-27-tasks-0119-0121-independent-review.md` — 0119: 64/100 REJECTED, 0121: 88/100 REJECTED
  - `docs/reports/review/0125-stream-repetition-guard-review.md` — 0125: 92/100 Elite
  - Task 0119 Review Trail: self-assessed Path-to-100 applied (2026-07-28), claims 95/100
  - Task 0121 Review Ledger: 88/100 from independent reviewer (2026-07-27)
- **Related reports**: none
- **Cross-task coupling**: 0119↔0125 share `combo/` subsystem semantics; no file overlap

---

## Per-Task Verdicts

| Task | Score | Verdict | Lane outcome |
|------|-------|---------|--------------|
| **0119** | **88/100** | **HELD_IN_REVIEW_PATH_TO_100** | remains in `02-doing/` |
| **0121** | **84/100** | **REJECTED_TO_DOING** | remains in `02-doing/` |
| **0122** | **52/100** | **REJECTED_TO_DOING** | remains in `02-doing/` |
| **0125** | **82/100** | **REJECTED_TO_DOING** | remains in `02-doing/` |

---

## Task 0119 — Fix NVIDIA NIM combo fallback when response has 0 output tokens

### Diff Ownership

- `open-sse/services/combo/validateQuality.ts` — lines 1-66 (TSDoc update, import cleanup), lines 326-342 (empty-streaming detection)
- `tests/unit/validate-quality-empty-streaming.test.ts` — created (87 lines, 6 tests)
- `.changelog/20260728-001000-0119-omniroute-combo-empty-streaming-fallback-builders.md` — created

### Verification Evidence

| Check | Command | Result |
|-------|---------|--------|
| Unit tests | `node --import tsx/esm --test tests/unit/validate-quality-empty-streaming.test.ts` | 6/6 pass |
| Regression | `node --import tsx/esm --test tests/unit/combo-quality-*.test.ts` | 12/12 pass |
| All touched | Combined run: 17/17 pass, 0 fail |
| Typecheck | `npm run typecheck:core` | PASS (0 errors) |
| Lint | `npx eslint --max-warnings=0` on touched files | PASS (0 errors, 0 warnings) |

### Delta Summary (vs prior 64/100 review)

#### Resolved Since Previous Review

- `RESOLVED`: Completion Evidence section is now filled with real command output, file:line ranges, test names, executor identity, and date.
- `RESOLVED`: Changelog entry created: `.changelog/20260728-001000-0119-omniroute-combo-empty-streaming-fallback-builders.md`.
- `RESOLVED`: `validateQuality.ts` TSDoc header updated to describe bounded peek + empty-streaming detection.
- `RESOLVED`: Unused import `isKnownNonClaudeStreamPayload` removed from `validateQuality.ts`.
- `RESOLVED`: Subtask and Exit Condition checkboxes marked.

#### Persistent Findings

- `EVIDENCE_GAP` (Downgraded from Blocker to Debt): Live test on `:23456` not performed; operator waiver noted. Acceptable for a bounded-peek change on a hot-path function that's already live; the 6 synthetic unit tests provide sufficient coverage.

#### New Findings

| ID | Class | Severity | Summary | Evidence |
|----|-------|----------|---------|----------|
| 0119-R2-F1 | `NEW` | Debt | `responsesApiOutputHasContent()` at lines 30, 39-40 uses `as Record<string, unknown>` and `as Record<string, string>` casts without `// SAFETY:` comments. Not introduced by this task (pre-existing), but blocks score > 90 under zero-tolerance `as` audit. | `validateQuality.ts:30,39-40` |
| 0119-R2-F2 | `NEW` | Debt | The Completion Evidence claims "18/18 PASS" including the `combo-quality-validator-reasoning` suite (12 tests), but the total 18 includes 6 new tests. The report should distinguish new vs regression (6 new + 12 regression = 18 total). Minor documentation accuracy issue. | `docs/tasks/02-doing/0119:164-165` |

### Path to 100 (0119)

1. Add `// SAFETY:` comments to `as` casts in `validateQuality.ts:30,39-40` (pre-existing — not introduced by this task, but good hygiene for the touched file).
2. Clarify test evidence: "6 new + 12 regression = 18 total PASS".
3. (Optional) Live smoke on `:23456` if the operator provides the combo config.

### Score Rationale

+94 base (implementation correct, tests robust, TSDoc updated, changelog present, all prior blockers resolved).
-6 for evidence precision debt (test count documentation, pre-existing `as` casts in touched file).
**= 88/100 — Good**

---

## Task 0121 — Port LM Arena executor modernization (PR #6280)

### Diff Ownership

- `open-sse/executors/lmarena.ts` — replaced (234 lines)
- `open-sse/executors/lmarena/{cookie,models,stream,response}.ts` — created (103+307+132+305 lines)
- `open-sse/services/lmarenaTlsClient.ts` — created (605 lines)
- `open-sse/config/providers/registry/lmarena/directModels.ts` — created (737 lines)
- `src/lib/providers/validation/webProvidersA.ts:611-679` — updated
- 6 test suites — created/modified

### Verification Evidence

| Check | Command | Result |
|-------|---------|--------|
| LMArena unit tests | `node --import tsx/esm --test tests/unit/lmarena-*.test.ts tests/unit/executor-lmarena.test.ts` | 38/38 pass, 9 suites |
| Typecheck | `npm run typecheck:core` | PASS (0 errors) |
| Lint | `npx eslint --max-warnings=0` on production files | PASS (0 errors, 0 warnings) |
| Lint (test) | `npx eslint` on `lmarena-validation.test.ts` | 3 warnings (explicit `any`) |
| Executor registration | `open-sse/executors/index.ts:52,147,215` | ✅ Confirmed |
| Provider registration | `open-sse/config/providers/index.ts:174,349` | ✅ Confirmed |
| Validation registration | `src/lib/providers/validation.ts:35,351` | ✅ Confirmed |

### Delta Summary (vs prior 88/100 review)

#### Persistent Findings

| ID | Class | Severity | Summary | Evidence |
|----|-------|----------|---------|----------|
| 0121-R2-F1 | `PERSISTENT` | Debt | 17 `as T` type assertions across production files lack `// SAFETY:` comments. All are guarded by runtime type checks (Low risk), but the project convention demands `// SAFETY:`. | `lmarena.ts:58,63,106,108,128,133,137`; `cookie.ts:87,96`; `models.ts:248`; `stream.ts:21,71,94,103`; `lmarenaTlsClient.ts:153,395`; `directModels.ts:695` |
| 0121-R2-F2 | `PERSISTENT` | **Blocker** | `.changelog/0121-omniroute-lmarena-pr6280-port.md` missing. No changelog entry exists for this task. | `ls .changelog/ \| grep 0121` → empty |
| 0121-R2-F3 | `PERSISTENT` / `EXTERNAL_BLOCKER` | Debt | Live arena test not performed (operator cookie required). Acceptable as EXTERNAL_BLOCKER. | task file notes |
| 0121-R2-F4 | `PERSISTENT` / `EVIDENCE_GAP` | Debt | Upstream diff evidence unavailable in workspace. Port fidelity not independently verifiable. | task requirement |

#### New Findings

| ID | Class | Severity | Summary | Evidence |
|----|-------|----------|---------|----------|
| 0121-R2-F5 | `NEW` | Debt | `tests/unit/lmarena-validation.test.ts` uses `{ apiKey }: any` at function signature and `error: any` catch. Introduces avoidable new `any` warnings. | lint output: 3 warnings |
| 0121-R2-F6 | `NEW` | Debt | Planning doc `0001-omniroute-web-providers-fix-plan.md` Fix 1 still contains stale "criar registry" instructions. | `docs/tasks/00-planning/0001:31-62` |
| 0121-R2-F7 | `NEW` | Info | Zero explicit `any` types in production files (0 in 7 files). This is elite-grade type safety for a port. | grep evidence |

#### Positive Assessment

**The production code is clean**: 0 explicit `any` in production, thorough TLS signal handling, abort propagation verified at 6 locations, no unhandled promise rejections, no `eval`/`new Function`, error sanitization via `sanitizeErrorMessage()`. The port is functionally sound.

### Path to 100 (0121)

1. **(Blocker)** Create `.changelog/0121-omniroute-lmarena-pr6280-port.md` and rebuild.
2. Add `// SAFETY:` comments to the 17 `as T` casts (or refactor the 2 in `lmarenaTlsClient.ts:153,395` that lack runtime guards).
3. Update planning doc Fix 1 to remove stale registry-creation instructions.
4. Clean up `lmarena-validation.test.ts` explicit `any` (replace with `unknown` + type guards).
5. Mark all completed subtasks/Exit Conditions checkboxes.

### Score Rationale

+92 base (production code is clean, tests comprehensive, wiring verified at 3 registration points).
-4 persistent: 17 `as T` without `// SAFETY:` (all low-risk but convention demands it).
-4 blocker: missing changelog entry.
**= 84/100 — Good (blocked by changelog)**

---

## Task 0122 — Port Kimi-web executor from upstream (Connect-RPC)

### Diff Ownership

- `open-sse/executors/kimi-web.ts` — replaced (210 lines)
- `open-sse/config/providers/registry/kimi/web/runtime.ts` — created (27 lines)
- `open-sse/config/providers/registry/kimi/web/index.ts` — updated (17 lines)
- `src/lib/providers/webCookieAuth.ts:113-143` — added `extractKimiAccessToken()`
- `src/lib/providers/validation/webProvidersA.ts:793+` — added `validateKimiWebProvider`
- `src/lib/providers/validation.ts:36,357` — registered in `SPECIALTY_VALIDATORS`
- `src/shared/constants/providers/web-cookie.ts:237-249` — updated
- `open-sse/services/tokenExtractionConfig.ts:194-204` — updated
- `src/shared/providers/webSessionCredentials.ts:153-159` — updated
- `tests/unit/executor-kimi-web.test.ts` — exists (28 lines) ⚠️ STALE
- `tests/unit/executor-kimi-web-decoder.test.ts` — exists (73 lines) ✅
- `tests/unit/kimi-web-models-discovery.test.ts` — **MISSING**

### Verification Evidence

| Check | Command | Result |
|-------|---------|--------|
| Kimi tests | `node --import tsx/esm --test tests/unit/executor-kimi-web.test.ts tests/unit/executor-kimi-web-decoder.test.ts` | 12/12 pass |
| Typecheck | `npm run typecheck:core` | PASS (0 errors) |
| Lint | `npx eslint --max-warnings=0` on production+test files | **FAIL** (3 warnings: explicit `any` in `kimi-web.ts`) |
| Executor registration | `open-sse/executors/index.ts:46,136` | ✅ Confirmed |
| Provider registration | `open-sse/config/providers/index.ts:47,220` | ✅ Confirmed |
| Validation registration | `src/lib/providers/validation.ts:36,357` | ✅ Confirmed |
| Other 3 Kimi providers | Untouched | ✅ Confirmed |

### Findings

| ID | Class | Severity | Summary | Evidence |
|----|-------|----------|---------|----------|
| 0122-F1 | `NEW` | **Critical** | `tests/unit/executor-kimi-web.test.ts:23` asserts `result.url.includes("kimi.moonshot.cn")` — the **dead domain**. The executor now uses `www.kimi.com`. The test "passes" only because the `try/catch` at lines 14-27 silently swallows the assertion error. **This is a phantom-passing test.** The test provides zero regression coverage for the ported code. | `tests/unit/executor-kimi-web.test.ts:14-27` — the `catch {}` at line 24 swallows **all** errors including assertion failures |
| 0122-F2 | `NEW` | **Critical** | `tests/unit/executor-kimi-web.test.ts:17` uses `model: "kimi-default"` — a stale model ID from the old executor. The new executor uses `k3` and `k2d6`. The test does not validate the new model resolution at all. | `tests/unit/executor-kimi-web.test.ts:17` |
| 0122-F3 | `NEW` | **Blocker** | `tests/unit/kimi-web-models-discovery.test.ts` — listed in task Exit Conditions but does **not exist on disk**. This is a missing required test file. | `ls tests/unit/kimi-web-models-discovery.test.ts` → ENOENT |
| 0122-F4 | `NEW` | **Blocker** | `.changelog/0122-omniroute-kimi-web-port.md` does not exist. Exit Condition requires it. | `ls .changelog/ \| grep 0122` → empty |
| 0122-F5 | `NEW` | **Blocker** | Completion Evidence section is entirely unfilled (placeholder text). | `docs/tasks/02-doing/0122:195-205` |
| 0122-F6 | `NEW` | High | 3 explicit `any` types in `open-sse/executors/kimi-web.ts`: line 74 (`Array<any>`), line 130 (`as any`), line 173 (`as any`). None have `// SAFETY:` comments. The `as any` at lines 130 and 173 defeat the type system entirely. | lint output + read |
| 0122-F7 | `NEW` | High | Streaming frame buffer uses `number[]` with `.push(...value)` + `.splice(0, n)` — O(n²) for large streams. V8's sparse array optimization will NOT help here because `push(...value)` spreads a `Uint8Array` into individual numbers. Should use a proper `Uint8Array` buffer with pointer tracking. | `kimi-web.ts:158,163,170` |
| 0122-F8 | `NEW` | Medium | `decodeConnectFrame()` at line 42 calls `buffer.buffer.slice(0)` which creates a **copy** of the entire ArrayBuffer just to create a DataView. Unnecessary — `new DataView(buffer.buffer, buffer.byteOffset)` would be zero-copy. | `kimi-web.ts:42` |
| 0122-F9 | `NEW` | Medium | Streaming path: if upstream sends a compressed frame (`compression !== 0`), line 167 `break`s out of the inner while loop but the unread data stays in the buffer forever. No frames after a compressed frame will ever be processed. This is a **silent data loss** scenario. | `kimi-web.ts:165-167` |
| 0122-F10 | `NEW` | Low | The upstream executor (~586 lines per task desc) was ported as 210 lines. Significant simplification is fine, but the task says "straight port — do not invent logic". The deviation from upstream should be documented. | Task says ~586 lines upstream, actual 210 lines |
| 0122-F11 | `NEW` | Low | `frame.buffer as ArrayBuffer` at line 107 uses an unsafe cast. `Uint8Array.buffer` returns `ArrayBufferLike`, not `ArrayBuffer`. Modern fetch may reject this. | `kimi-web.ts:107` |

### Path to 100 (0122)

1. **(Critical)** Rewrite `tests/unit/executor-kimi-web.test.ts`: fix dead-domain assertion, fix stale model ID, **remove the bare `catch {}`** that swallows assertion errors.
2. **(Blocker)** Create `tests/unit/kimi-web-models-discovery.test.ts` with `resolveKimiModelId()` tests for k3/k2d6 coverage.
3. **(Blocker)** Create `.changelog/0122-omniroute-kimi-web-port.md` and rebuild.
4. **(Blocker)** Fill the Completion Evidence section with real command output.
5. **(High)** Replace `as any` casts at lines 130, 173 with proper type guards or `unknown` + narrowing.
6. **(High)** Replace `Array<any>` at line 74 with `Array<{ role: string; content: string | unknown[] }>`.
7. **(High)** Replace `number[]` frame buffer with `Uint8Array` circular buffer.
8. **(Medium)** Fix `decodeConnectFrame()` to use `new DataView(buffer.buffer, buffer.byteOffset)`.
9. **(Medium)** Handle compressed frames gracefully (skip the frame, not break the loop).
10. **(Low)** Document the deviation from upstream (210 vs ~586 lines) in Completion Evidence.

### Score Rationale

+70 base (executor works, decoder tests good, helper functions tested, registration verified).
-18 critical: phantom test + missing test file + unfilled evidence + missing changelog.
**= 52/100 — Serious**

---

## Task 0125 — Stream repetition guard

### Diff Ownership

- `open-sse/services/streamRepetitionGuard.ts` — created (67 lines)
- `open-sse/utils/stream.ts` — modified (import + options + wiring at 5 call sites)
- `open-sse/utils/streamHandler.ts` — modified (`isClientDisconnectError` exclusion)
- `open-sse/services/combo/targetExhaustion.ts` — modified (`isRepetitionFailure` + exclusion)
- `open-sse/services/combo/comboPredicates.ts` — modified (`isRepetitionFailure?` in breaker predicate)
- `open-sse/services/comboConfig.ts` — modified (default `enableRepetitionGuard: false`)
- `src/shared/validation/schemas/combo.ts` — modified (Zod schema: `enableRepetitionGuard: z.boolean().optional()`)
- `tests/unit/stream-repetition-guard.test.ts` — created
- `tests/unit/combo-repetition-fallback.test.ts` — created
- `.changelog/20260727-140000-0125-omniroute-stream-repetition-guard-builders.md` — created

### Verification Evidence

| Check | Command | Result |
|-------|---------|--------|
| Guard + combo tests | `node --import tsx/esm --test tests/unit/stream-repetition-guard.test.ts tests/unit/combo-repetition-fallback.test.ts` | 11/11 pass |
| Lint | `npx eslint --max-warnings=0` on all touched files | PASS (0 errors, 0 warnings) |
| Typecheck | `npm run typecheck:core` | PASS (0 errors) |

### Delta Summary (vs prior 92/100 review)

The prior review gave 92/100 and noted two improvements: (a) code comments for future `checkRepetition()` connections, (b) integration-depth test for combo fallback. This re-review validates those and surfaces a **new critical integration finding**.

#### Persistent Findings (from prior 92/100)

- `PERSISTENT` (Debt): Code comment enumerating required `checkRepetition()` connections when new SSE deltas are added — not yet addressed.
- `PERSISTENT` (Debt): Integration-depth test for wrapped execution-path assertion — not yet addressed.

#### New Findings

| ID | Class | Severity | Summary | Evidence |
|----|-------|----------|---------|----------|
| 0125-R2-F1 | `NEW` | **High** | **Combo-level toggle is dead code.** `comboConfig.ts:56` defaults `enableRepetitionGuard: false` and `combo.ts` Zod schema validates it, but the combo config value is **never propagated** to `createSSEStream()` or to the `body` parameter. The `stream.ts:735-737` runtime check reads `enableRepetitionGuard` from the `body` object (raw request) or the `StreamOptions` parameter — neither of which receives the combo-level config. The two wrapper functions `createStreamWithTranslation()` and `createPassthroughStreamWithLogger()` (lines 2729, 2759) do **not** pass `enableRepetitionGuard` to `createSSEStream()`. **Result**: the combo-level toggle documented in the task is unreachable. The guard can only be activated per-request via `body.enableRepetitionGuard: true` — which is undocumented and violates the task's specification that the toggle is "combo-level". | `comboConfig.ts:56`, `stream.ts:735-737`, `stream.ts:2729-2744`, `stream.ts:2747-2772` — no `enableRepetitionGuard` propagation |
| 0125-R2-F2 | `NEW` | Medium | The Compliance Checklist in the task file (line 163) says "Zod Validation: combo config toggle is Zod-validated" — this is true (`combo.ts:180`), but the Zod validation only means the field is _accepted_ in the schema; it does not mean the field _works_ at runtime. The checklist item is misleading. | task file vs runtime evidence |
| 0125-R2-F3 | `NEW` | Debt | The guard counts consecutive identical chunks, but the `check()` function resets `consecutiveCount` to 1 (not 0) on a new chunk. This means the first occurrence of a chunk counts as 1, and `historySize` defaults to 3, so the guard triggers on the **3rd** identical chunk (not 2nd as the user requested "≥2x repetition"). The task documents this as intentional ("more conservative"), but the objective says "≥2x repetition". Minor spec drift. | `streamRepetitionGuard.ts:48-53` vs task objective line 16 |

### Path to 100 (0125)

1. **(High — functional gap)** Wire the combo-level `enableRepetitionGuard` config into the stream runtime. Either:
   (a) Pass it through `chatCore.ts` → `StreamOptions` → `createSSEStream()`, or
   (b) Inject it into the request `body` from combo config before the stream is created.
2. Add code comments in `stream.ts` enumerating required `checkRepetition()` connections for future SSE delta formats.
3. Add an integration-style test asserting the combo-level toggle reaches `stream.ts` and activates the guard.
4. (Optional) Document the spec drift (3 consecutive vs ≥2x) explicitly in the module header.

### Score Rationale

+92 base (clean implementation, good test coverage, Zod schema, lint-clean, well-documented module).
-10 high: combo-level toggle is dead code (feature doesn't work as specified).
**= 82/100 — Good (blocked by toggle wiring)**

---

## Cross-Task Blast Radius

### Shared files

| Path | Tasks | Conflict Risk |
|------|-------|--------------|
| `open-sse/services/combo/validateQuality.ts` | 0119 (lines 326-342) | Low — isolated to empty-streaming branch |
| `open-sse/utils/stream.ts` | 0125 (guard wiring at 5 sites) | Low — additions only, no semantic overlap |
| `open-sse/services/combo/targetExhaustion.ts` | 0125 (repetition exclusion) | Low — additions only |
| `src/lib/providers/validation/webProvidersA.ts` | 0121 (lmarena validation), 0122 (kimi validation) | Low — different functions, different line ranges |
| `src/lib/providers/validation.ts` | 0121, 0122 (register in SPECIALTY_VALIDATORS) | Low — additive entries |

### Shared interfaces/types

- `ComboExhaustionSets` (targetExhaustion.ts) — touched by 0125 only (additive exclusion).
- `StreamOptions` (stream.ts) — touched by 0125 only (additive `enableRepetitionGuard?` field).
- No cross-task interface conflicts.

### Regression risk

- Accepting only 0119 without 0125: safe — they touch disjoint code.
- Accepting only 0125 without 0119: safe — stream guard is opt-in default-off.
- Accepting only 0121 without 0122: safe — different providers.
- Task 0122 is the weakest — its phantom test could mask a regression if accepted.

### Diff ownership map

| File/Symbol | Owning Task |
|-------------|-------------|
| `validateQuality.ts:326-342` | 0119 |
| `streamRepetitionGuard.ts` | 0125 |
| `stream.ts:735-779` | 0125 |
| `targetExhaustion.ts:36-38,126` | 0125 |
| `comboPredicates.ts:158,165` | 0125 |
| `comboConfig.ts:56` | 0125 |
| `combo.ts schema:180` | 0125 |
| `executors/kimi-web.ts` | 0122 |
| `kimi/web/runtime.ts` | 0122 |
| `kimi/web/index.ts` | 0122 |
| `webCookieAuth.ts:113-143` | 0122 |
| `executors/lmarena.ts` | 0121 |
| `executors/lmarena/*.ts` | 0121 |
| `lmarenaTlsClient.ts` | 0121 |
| `directModels.ts` | 0121 |

---

## Evidence Reviewed

- Task files: 0119, 0121, 0122, 0125 — all in `docs/tasks/02-doing/`
- Source/test files: 40+ files read and grepped across all 4 tasks
- Runtime wiring: executor registration (executors/index.ts), provider registration (config/providers/index.ts), validation registration (validation.ts) — confirmed for 0121 and 0122
- Commands run:
  - `node --import tsx/esm --test tests/unit/validate-quality-empty-streaming.test.ts` — 6/6 pass
  - `node --import tsx/esm --test tests/unit/lmarena-*.test.ts tests/unit/executor-lmarena.test.ts` — 38/38 pass
  - `node --import tsx/esm --test tests/unit/executor-kimi-web.test.ts tests/unit/executor-kimi-web-decoder.test.ts` — 12/12 pass (**phantom**)
  - `node --import tsx/esm --test tests/unit/stream-repetition-guard.test.ts tests/unit/combo-repetition-fallback.test.ts` — 11/11 pass
  - `npm run typecheck:core` — PASS
  - `npx eslint --max-warnings=0` on all touched files — PASS for 0119/0121/0125; FAIL for 0122 (3 `any` warnings)
- Commands not run: `npm run test:all` (scope does not warrant full suite), live tests on `:23456` (no operator combo config available)
- Subagent investigation: codebase-investigator provided detailed `as T` audit for 0121 (17 casts, 0 `// SAFETY:`, 0 explicit `any` in production)
- Gortex graph: `get_editing_context` on `validateQuality.ts` confirmed caller (`executeRuntimeUnitCombo`) and callee graph

---

## Reviewer Recommendations

1. **Task 0122 requires significant rework** — the phantom test is a critical integrity issue. The test passes due to a bare `catch {}` that swallows assertion errors, which means the test provides **zero coverage** for the ported code.
2. **Task 0125 works correctly but the documented toggle is unreachable** — this is a functional gap that should be fixed before closing.
3. **Task 0121 is close to done** — the missing changelog is the only hard blocker; `// SAFETY:` comments are important but low-risk.
4. **Task 0119 has resolved all prior blockers** — remaining items are documentation polish and pre-existing debt in the touched file.
5. All tasks remain in `docs/tasks/02-doing/` per user instruction. No lane moves performed.
