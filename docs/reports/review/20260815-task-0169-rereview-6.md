# Independent Delta Re-review Report — Task 0169: BYO proxy validation and free-pool non-goal documentation

## Review identity and lineage

- **Task:** `docs/tasks/02-doing/0169-omniroute-byo-proxy-validation-free-pool-nongoal.md`
- **Reviewer:** `builders` (independent review lane)
- **Re-review date:** 2026-08-15
- **Previous report:** [`20260815-task-0169-rereview-5.md`](20260815-task-0169-rereview-5.md) — **REJECTED, 97/100**
- **Review mode:** read-only application-source and verification review; no application source files were modified by the reviewer.
- **Scope:** final Mimocode per-account dispatcher correction, fresh regression suites, core typecheck, production-file lint, LSP diagnostics, direct DNS-rebinding runtime probe, and async call-site audit.

## Verdict

### **100/100 — APPROVED; promotion authorized**

The remaining P1 from the prior re-review is resolved. `MimocodeExecutor.getProxyDispatcher()` is now asynchronous and awaits `assertValidProxyHost()` for each credential-provided proxy hostname before calling the synchronous `createProxyDispatcher()` factory. `fetchWithProxy()` awaits the dispatcher lookup, and `getJwtForAccount()` awaits the same path before bootstrap. A direct runtime probe injected a DNS answer of `127.0.0.1` for a public-looking per-account hostname and observed fail-closed rejection before dispatcher construction or network activity.

The previously corrected request-context, patched-fetch, fallback, persistence, promotion, egress, free-pool, and locality paths remain present and green. Fresh verification passes: the focused trust suite is 51/51, the proxy-wide suite is 273/273, the Mimocode executor suite is 40/40, `npm run typecheck:core` exits 0, production proxy files lint cleanly, and LSP diagnostics are empty for the reviewed implementation and focused test files.

Promotion is approved. The task may move from `docs/tasks/02-doing/` to `docs/tasks/03-review/`.

## Delta disposition

| Prior finding | Status | Current evidence |
|---|---|---|
| Trailing-dot private-host bypass | **RESOLVED** | `normalizeProxyHostname()` strips trailing dots before literal checks; focused tests pass. |
| Proxy-host DNS rebinding on named HTTP paths | **RESOLVED** | Shared `assertValidProxyHost()` resolves hostnames with `all: true` and rejects private, link-local, metadata, mixed, empty, and failed answers. |
| Direct promotion helper validation | **RESOLVED** | `promoteFreeProxyToPool()` validates before registry access and writes; injected rebinding test passes with no registry write. |
| Direct registry persistence validation | **RESOLVED** | Create, create-and-assign, update, update-and-assign, and legacy migration paths use the shared asynchronous guard. |
| Stored-proxy egress diagnostics/pool validation | **RESOLVED** | Egress probe, diagnostics, and pool validation guard before probing or dispatcher construction. |
| Default free-pool provider posture | **RESOLVED** | Providers require explicit enablement; no-env test returns no enabled providers. |
| Proxy/free-pool route locality | **RESOLVED** | Central `LOCAL_ONLY_API_PREFIXES` covers reviewed proxy registry, proxy config, free-proxy, and oneproxy paths. |
| General stored-proxy request dispatch | **RESOLVED** | `runWithProxyContext()` and `patchedFetch()` validate before reachability, callback, cache/dispatcher construction, and fetch. |
| Proxy fallback candidate dispatch | **RESOLVED** | `testSingleProxy()` validates candidates before dispatcher construction and network probing. |
| Direct non-context dispatcher callers | **RESOLVED** | `MimocodeExecutor.getProxyDispatcher()` now awaits host validation before `createProxyDispatcher()`. |
| Completion evidence and changelog verification | **RESOLVED** | Task evidence records the final 51/51, 273/273, 40/40, typecheck, and lint results; canonical changelog verification is checked. |

## Fresh verification evidence

| Check | Result |
|---|---|
| `node --import tsx/esm --test tests/unit/proxy-trust-and-validation.test.ts` | **PASS** — 51 tests passed, 0 failed, 0 skipped |
| `node --import tsx/esm --test tests/unit/proxy-*.test.ts` | **PASS** — 273 tests passed, 0 failed, 0 skipped |
| `node --import tsx/esm --test tests/unit/mimocode-executor.test.ts` | **PASS** — 40 tests passed, 0 failed, 0 skipped |
| `npm run typecheck:core` | **PASS** — exit 0 |
| Scoped ESLint on production proxy implementation files | **PASS** — no errors or warnings |
| LSP diagnostics for Mimocode, proxy fetch, fallback, dispatcher, host guard, egress, persistence, route guard, and focused trust test | **PASS** — 0 diagnostics each |
| Repository index health | **PASS** — 100.0%, 0 stale files, 0 parse failures |
| Direct Mimocode DNS-rebinding probe | **PASS** — injected `127.0.0.1` answer rejected; `lookupCalls=1`, dispatcher construction not reached, network calls=0 |

The broad lint invocation over both production and test files reports 65 existing `@typescript-eslint/no-explicit-any` warnings in test code and no errors. The production implementation lint gate is clean; the warnings do not affect the security correction or the exit-code gate.

## Direct Mimocode runtime probe

The probe used the existing `setGlobalProxyLookupForTests()` seam, configured a credential-shaped per-account proxy for `rebind-direct-probe.example.com`, and returned `127.0.0.1` for its DNS answer. It called the private `getProxyDispatcher()` path directly after setting `ALLOW_LOCAL_PROXIES` absent.

```json
{
  "outcome": "rejected",
  "message": "Proxy host resolves to a blocked private address (DNS rebinding detected): 127.0.0.1",
  "lookupCalls": 1,
  "dispatcherConstructionReached": false,
  "networkCalls": 0
}
```

The source ordering provides the required boundary proof: `new URL()` and `assertValidProxyHost()` complete before `createProxyDispatcher(proxyUrl)`. Since the injected resolver rejects, the factory call and the subsequent undici/bootstrap paths are unreachable. The focused regression test additionally exercises executor failure handling and confirms a 502 fail-closed result.

## Async call-site audit

The signature change is fully propagated:

1. `MimocodeExecutor.getProxyDispatcher()` returns `Promise<Dispatcher | undefined>` and awaits `assertValidProxyHost()`.
2. `MimocodeExecutor.fetchWithProxy()` awaits `this.getProxyDispatcher(fingerprint)` before undici fetch.
3. `MimocodeExecutor.getJwtForAccount()` awaits `this.getProxyDispatcher(account.fingerprint)` before `bootstrapJwt()`.
4. No remaining direct call site invokes the async method without `await`.

The `ALLOW_LOCAL_PROXIES === "true"` bypass remains explicit and consistent with the other proxy validation paths; with the bypass absent, public-looking hostnames are DNS-validated before dispatch.

## Verification objective audit

### 1. Trust documentation and links — **PASS (20/20)**

`docs/security/PROXY_TRUST.md` documents the no-default-free-list non-goal, staging-only/untrusted scrapers, BYO production path, SSRF defenses, Hard Rule #20, and authz boundaries. `AGENTS.md` and the proxy Documentation tab reference it.

### 2. Schema, literal, and persistence enforcement — **PASS (25/25)**

Literal normalization covers trailing dots and bracketed IPv6. Schemas, registry persistence, migration, promotion, and configuration paths reject private/local forms and DNS-rebinding answers before writes or dispatch.

### 3. DNS rebinding and runtime dispatch enforcement — **PASS (25/25)**

Named routes, direct DB helpers, egress diagnostics, normal stored-proxy request context, patched fetch, fallback candidates, and direct Mimocode per-account dispatch all invoke the asynchronous host guard before side effects. The final direct Mimocode probe closes the last previously unguarded boundary.

### 4. Free-pool staging/filter/default posture — **PASS (10/10)**

All providers require explicit opt-in, and private/loopback/link-local entries are filtered before staging. The focused suite covers the default-disabled and explicit-enable behavior.

### 5. Authorization and `LOCAL_ONLY` route enforcement — **PASS (10/10)**

The central locality policy covers the reviewed proxy registry, proxy configuration, free-proxy, and oneproxy routes; the route matrix and management-auth tests pass.

### 6. Verification/evidence/changelog integrity — **PASS (10/10)**

Final focused, proxy-wide, executor, typecheck, lint, LSP, repository-health, and direct runtime evidence is current. The task closure matrix includes the direct Mimocode boundary, and the canonical changelog verification item is checked.

## Score breakdown

| Dimension | Available | Earned | Rationale |
|---|---:|---:|---|
| Trust documentation and links | 20 | 20 | Required trust boundary, non-goals, BYO path, SSRF, and links are present and tested. |
| Schema/literal/persistence enforcement | 25 | 25 | Literal normalization, routes, persistence, promotion, and migration protections are verified. |
| DNS rebinding and runtime dispatch | 25 | 25 | Every identified dispatcher and proxy lifecycle boundary is guarded, including direct Mimocode dispatch. |
| Free-pool staging/filter/default posture | 10 | 10 | Provider filtering and explicit opt-in defaults are verified. |
| Authz and `LOCAL_ONLY` route enforcement | 10 | 10 | Central locality policy and regression matrix pass. |
| Verification/evidence/changelog integrity | 10 | 10 | Fresh evidence covers all closure-matrix requirements and the changelog is checked. |
| **Total** | **100** | **100** | **APPROVED** |

## Promotion decision

- **Promotion:** approved.
- **Current lane:** `docs/tasks/02-doing/` before move.
- **Destination lane:** `docs/tasks/03-review/`.
- **Application source changes by reviewer:** none.
- **Residual blockers:** none identified for Task 0169's stated scope.
