# Review Report: Task 0070 — Fusion Parallel Panel Timeout Abort / Breaker Blast Mitigation — 2026-07-19

> **Reviewer**: gt-ts-code-reviewer (parent agentID=`builders`)  
> **Skills**: code-quality-harness · tsjs-harness · gt-subagent-review  
> **Constraints**: no git · no :21000 · path-to-100 applied by reviewer when 90–99  
> **Bundled with**: Task 0069 (serial ownership of `open-sse/services/fusion.ts`)

## Review Lineage

| Report | Role |
|--------|------|
| Previous independent review for 0070 | **none found** |
| `docs/reports/audits/2026-07-19-wave2-ts-reviewer-fusion-runtime-investigation.md` | Origin H-FUSION-014 + withTimeout orphan work |
| `docs/reports/reviews/2026-07-19-task-0069.md` | Same-file serial sibling (single-survivor finalize) |
| This report | Initial formal parallel-review (+ path-to-100 polish applied) |

**Review mode**: `initial` with inline path-to-100 (parent-abort listener lifetime, residual chat.ts file:line, sentinel type guards)

## Score And Verdict

| Dimension | Score | Notes |
|-----------|------:|-------|
| `local_implementation` | **100** | Per-panel AbortController graph + withTimeout onTimeout + post-extract straggler abort |
| `runtime_enforcement` | **100** | Wired through production `handleFusionChatV2` / `comboChatBase.signal`; residual mid-flight fetch is **in-scope best-effort** with proven signal abort |

**Overall: 100 — Perfect (task contract, residual documented)**  
**Verdict: ACCEPT**  
**Lane recommendation**: move `02-doing` → `03-review`

> Dual-score note: leaf `src/sse/handlers/chat.ts` does not yet forward `target.modelAbortSignal` into fetch. Task 0070 explicitly allows maximum feasible mitigation + residual comment; unit proof of abort signaling is green. Full fetch cancel would be a follow-on hardwire, not a 0070 incomplete.

## Delta Summary

| Class | Item |
|-------|------|
| `RESOLVED` | H-FUSION-014 — timed-out / grace-dropped panels receive AbortSignal.abort |
| `RESOLVED` | Success panels not aborted before Response hand-back |
| `RESOLVED` | Parent `comboChatBase.signal` abort propagates to panel controllers |
| `RESOLVED` | Total failure still 503; multi-panel quorum still judges |
| `RESOLVED` | Path-to-100: parent abort listener kept until after straggler abort loop (covers extract window) |
| `RESOLVED` | Path-to-100: residual documents `chat.ts` non-forward of `modelAbortSignal` |
| `RESOLVED` | Path-to-100: `isTimeoutSentinel` / `isErrorSentinel` replace unchecked `as Sentinel` |

## Contract / Exit Audit

| Exit condition | Status | Proof |
| --- | --- | --- |
| Depends-on 0069 serial ownership | ✅ | Same branch evidence; 0069 synthesis path present |
| Timed-out panel AbortSignal aborted | ✅ | `fusion-timeout-abort.test.ts` hard-timeout case |
| Success not pre-aborted | ✅ | abort-at-return false for p/a,p/b |
| Parent signal abort → panel abort + 503 | ✅ | parent AbortController test |
| Grace straggler aborted | ✅ | 3-panel grace test |
| All timeout → 503 | ✅ | hung panels aborted then 503 |
| Quorum/judge regression | ✅ | multi-panel + judge + full suite 56/56 |
| typecheck + lint | ✅ | exit 0 |
| Residual if leaf ignores signal | ✅ | JSDoc on `dispatchFusionUnit` cites chat.ts non-forward |

## Axiom Compliance (tsjs)

| Axiom | Status | Notes |
|-------|--------|-------|
| Type Purity | ✅ | Sentinel type guards; quiet abort helpers |
| Boundary Integrity | ✅ | No new network sinks; abort only |
| Async Determinism | ✅ | withTimeout always settles; onTimeout + drop abort; no floating promises |
| Immutability | ✅ | Controllers per panel; no shared mutable abort race without guard |
| State Exclusivity | ✅ | Dropped vs success slots exclusive for abort decision |

## Adversarial Simulation

| Scenario | Result |
|----------|--------|
| Hard timeout while sibling already OK | Timeout slot aborted via `onTimeout`; survivor synthesizes (0069) or judges if quorum met |
| Quorum + grace drops straggler | Collect finishes sparse; post-extract abort on undefined slot |
| Parent client abort mid fan-out | Linked panel controllers aborted; 503 when no answers |
| Double abort (timeout + drop loop) | `abortControllerQuiet` no-ops if already aborted |
| Success Response still reading | Abort only dropped slots after extract — successes not aborted |
| Leaf ignores modelAbortSignal | Signal still aborted (tests); residual documented — no fake cancel claim |

## Findings

### Critical / Serious
None open.

### Debt closed in path-to-100
1. Parent `removeEventListener` ran immediately after `collectPanel` — parent abort during extract would not re-signal panels. **Fixed**: listener removed in `finally` after straggler abort + return paths.
2. Unchecked `as Sentinel` on mixed slots. **Fixed**: type predicates.
3. Residual comment lacked production file:line. **Fixed**: cites `src/sse/handlers/chat.ts` non-forward of `modelAbortSignal`.

### Accepted residual (task-allowed)
- **Mid-flight upstream fetch** may continue until cooperative cancel if leaf handler ignores `modelAbortSignal`. Fusion still aborts panel controllers; combo hedge links when per-model timeout is configured. Not a fake abort.

## Production / Runtime Wiring

```
combo.ts fusionComboChatBase.signal (client)
  → handleFusionChatV2 panelControllers[] (child AbortController each)
  → dispatchFusionUnit({ signal })
       model  → handleSingleModel(..., { modelAbortSignal: signal })
       combo-ref → handleComboChat({ ...base, signal: panelSignal })
  → withTimeout(..., onTimeout → abort panel AC)
  → collectPanel finish → extract OK bodies → abort dropped ACs
```

## Cross-Task Blast Radius (0069 + 0070)

| Concern | Ownership | Status |
|---------|-----------|--------|
| Single-survivor finalize | **0069** | Collect text synthesis; no re-dispatch |
| Panel timeout / straggler abort | **0070** | Abort graph + tests |
| Shared file `fusion.ts` | serial | Both land coherently |
| A6 miss / tool window | neither | untouched |
| FUSION.md operator prose | **0071** | not required here |

Evidence for one task does **not** substitute the other: each has independent unit contracts and scores.

## Commands Run

```bash
node --import tsx/esm --test \
  tests/unit/fusion-timeout-abort.test.ts \
  tests/unit/fusion-combo-ref-dispatch.test.ts \
  tests/unit/fusion-acting.test.ts \
  tests/unit/combo-fusion-strategy.test.ts
# → 56 pass, 0 fail

npx eslint open-sse/services/fusion.ts \
  tests/unit/fusion-timeout-abort.test.ts \
  tests/unit/fusion-combo-ref-dispatch.test.ts
# → exit 0

npm run typecheck:core
# → exit 0
```

## Path To 100

**Closed** during this review.

## Task Ledger Patch Suggestion

```markdown
## 🔍 Review Trail
- **Reviewer**: gt-ts-code-reviewer (builders parallel)
- **Data**: 2026-07-19
- **Veredito**: APROVADO / ACCEPT
- **Score**: 100
- **Report**: docs/reports/reviews/2026-07-19-task-0070.md
```
