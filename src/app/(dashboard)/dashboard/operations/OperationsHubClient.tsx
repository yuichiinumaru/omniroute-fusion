/**
 * Re-export: Operations hub cards live under the EPIC-20 shell tree.
 * Legacy path `/dashboard/operations` redirects; this file keeps import stability.
 */
export { default } from "../../operations/OperationsHubClient";
