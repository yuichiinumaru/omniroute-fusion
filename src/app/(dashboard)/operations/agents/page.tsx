import AgentsFusionClient from "./AgentsFusionClient";

/**
 * EPIC-20 T20-E / Task 0090 — `/operations/agents` fusion host.
 * Static segment beats `[segment]` for this peer. Shell topbar is layout-owned.
 */
export default function OperationsAgentsPage() {
  return <AgentsFusionClient />;
}
